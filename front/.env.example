# Front — copie para .env (opcional; vazio = mesma origem /api)
# Em dev o proxy do vite já resolve; só defina se o backend estiver noutra URL.
# VITE_API_URL=https://sua-api.vercel.app
VITE_API_URL=
